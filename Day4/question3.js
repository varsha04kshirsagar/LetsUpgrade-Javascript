//all the products you want to buy.
let shoppingList = ['Cabbage', 'Turnip', 'Radish', 'Carrot', 'sugar'];
console.log(shoppingList);
//want to add a few more
let newshoppingList = ['coffee powder', 'peanut'];
console.log(newshoppingList);
// add all products in shopping baket

let shoppingBasket = shoppingList.concat(newshoppingList);
console.log(shoppingBasket);